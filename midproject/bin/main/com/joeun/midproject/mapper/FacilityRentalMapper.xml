<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace="매퍼 인터페이스 경로" -->
<mapper namespace="com.joeun.midproject.mapper.FacilityRentalMapper">

    <!-- 게시글 목록 -->
    <select id="list" resultType="FacilityRental">
        SELECT *
        FROM facility_rental
        ORDER BY reg_date
    </select>

    <!-- 게시글 조회 -->
    <select id="select" resultType="FacilityRental">
        SELECT *
        FROM facility_rental
        WHERE fr_no = #{frNo}
    </select>

    <!-- 게시글 등록 -->
    <insert id="insert">
        INSERT INTO facility_rental(title, writer, username, content, price, location, address, live_date, phone, thumbnail, account )
        VALUES (#{title}, #{writer}, #{username}, #{content}, #{price}, #{location}, #{address}, #{live_date}, #{phone}, #{thumbnail}, #{account})
    </insert>

    <!-- 게시글 수정 -->
    <update id="update">
        UPDATE board
           SET title = #{title}
              ,content = #{content}
              ,price = #{price}
              ,location = #{location}
              ,address = #{address}
              ,live_date = #{liveDate}
              ,thumbnail = #{thumbnail}
              ,account = #{account}
        WHERE fr_no = #{frNo}
    </update>

    <!-- 게시글 삭제 -->
    <delete id="delete">
        DELETE FROM board
        WHERE board_no = #{boardNo}
    </delete>

    <!-- 게시글 번호 최대값 -->
    <select id="maxPK" resultType="int">
        SELECT MAX(fr_no)
        FROM facility_rental
    </select>

</mapper>